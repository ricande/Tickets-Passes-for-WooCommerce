{
  "name": "tpfw/tickets-passes-for-woocommerce",
  "description": "Optional test entry point. PHPUnit is fetched by tests/ensure-phpunit.sh (not Composer). The plugin does not require Composer to run.",
  "scripts": {
    "test": "bash tests/run.sh"
  }
}
